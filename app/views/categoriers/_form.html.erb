<%= form_with(model: categorier) do |form| %>
  <% if categorier.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(categorier.errors.count, "error") %> prohibited this categorier from being saved:</h2>

      <ul>
        <% categorier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
  <%= form.label :category, style: "display: block" %>
  <%= form.select :category_id, options_for_select(@categories.collect { |cat| [cat.name, cat.id] } ) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
